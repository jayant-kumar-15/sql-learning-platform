/*
 * ============================================================
 * FILE PATH: frontend/admin/admin-login.css
 * ============================================================
 * PURPOSE
 * -------
 * Minimal, professional styling for the private admin login.
 * Public users are never linked to this page from the homepage.
 * ============================================================
 */

:root {
    --bg: #07111f;
    --surface: #0d1b2e;
    --border: rgba(255,255,255,.1);
    --text: #edf4ff;
    --muted: #9fb0c5;
    --accent: #55c7ff;
    --danger: #ff7b88;
}

* { box-sizing: border-box; }

body {
    margin: 0;
    min-height: 100vh;
    display: grid;
    place-items: center;
    padding: 20px;
    color: var(--text);
    background:
        radial-gradient(circle at top right, rgba(85,199,255,.13), transparent 35%),
        var(--bg);
    font-family: Inter, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
}

.login-shell { width: min(100%, 480px); }

.login-card {
    padding: 34px;
    border: 1px solid var(--border);
    border-radius: 22px;
    background: rgba(13,27,46,.94);
    box-shadow: 0 30px 80px rgba(0,0,0,.3);
}

.brand {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 34px;
}

.brand-mark {
    width: 48px;
    height: 48px;
    display: grid;
    place-items: center;
    border-radius: 14px;
    background: var(--accent);
    color: #03111c;
    font-weight: 900;
}

.brand strong,
.brand small { display: block; }
.brand small { color: var(--muted); margin-top: 3px; }

.eyebrow {
    color: var(--accent);
    font-size: 12px;
    font-weight: 800;
    letter-spacing: .16em;
}

h1 { margin: 9px 0 8px; font-size: 36px; }
.intro { color: var(--muted); line-height: 1.6; margin-bottom: 26px; }

form { display: grid; gap: 18px; }

label {
    display: grid;
    gap: 8px;
    color: var(--text);
    font-weight: 650;
    font-size: 14px;
}

input {
    width: 100%;
    border: 1px solid var(--border);
    border-radius: 11px;
    padding: 13px 14px;
    color: var(--text);
    background: rgba(255,255,255,.04);
    outline: none;
}

input:focus { border-color: rgba(85,199,255,.65); }

.password-field { position: relative; }
.password-field input { padding-right: 70px; }

#togglePassword {
    position: absolute;
    right: 8px;
    top: 7px;
    border: 0;
    padding: 7px 9px;
    border-radius: 8px;
    background: transparent;
    color: var(--accent);
}

.submit-button {
    border: 0;
    border-radius: 12px;
    padding: 13px 16px;
    background: var(--accent);
    color: #03111c;
    font-weight: 850;
}

.submit-button:disabled { opacity: .65; cursor: wait; }

.status { min-height: 20px; margin: 0; color: var(--danger); }
.back-link { display: block; margin-top: 25px; color: var(--muted); text-decoration: none; }
.back-link:hover { color: var(--accent); }

@media (max-width: 480px) {
    .login-card { padding: 24px; }
    h1 { font-size: 30px; }
}
