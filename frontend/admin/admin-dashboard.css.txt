/*
 * ============================================================
 * FILE PATH: frontend/admin/admin-dashboard.css
 * ============================================================
 * PURPOSE
 * -------
 * Styling for the private administrator dashboard.
 * ============================================================
 */

:root {
    --bg: #07111f;
    --surface: #0d1b2e;
    --surface-2: #11233a;
    --border: rgba(255, 255, 255, 0.09);
    --text: #edf4ff;
    --muted: #9fb0c5;
    --accent: #55c7ff;
    --success: #54d39a;
    --danger: #ff7b88;
    --radius: 18px;
}

* {
    box-sizing: border-box;
}

body {
    margin: 0;
    min-height: 100vh;
    font-family: Inter, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    background: var(--bg);
    color: var(--text);
}

button,
a {
    font: inherit;
}

button {
    cursor: pointer;
}

.hidden {
    display: none !important;
}

.admin-shell {
    min-height: 100vh;
    display: grid;
    grid-template-columns: 250px minmax(0, 1fr);
}

.admin-sidebar {
    padding: 24px 16px;
    border-right: 1px solid var(--border);
    background: rgba(6, 16, 29, 0.95);
    display: flex;
    flex-direction: column;
    gap: 30px;
}

.admin-brand {
    display: flex;
    align-items: center;
    gap: 12px;
}

.brand-mark {
    width: 46px;
    height: 46px;
    display: grid;
    place-items: center;
    border-radius: 13px;
    background: var(--accent);
    color: #03111c;
    font-weight: 900;
}

.admin-brand strong,
.admin-brand small {
    display: block;
}

.admin-brand small {
    margin-top: 3px;
    color: var(--muted);
}

.admin-sidebar nav {
    display: grid;
    gap: 8px;
}

.nav-item,
.logout-button {
    width: 100%;
    border: 0;
    border-radius: 12px;
    padding: 12px 14px;
    color: var(--muted);
    background: transparent;
    text-align: left;
}

.nav-item:hover,
.nav-item.active {
    color: var(--text);
    background: var(--surface-2);
}

.logout-button {
    margin-top: auto;
    border: 1px solid var(--border);
    text-align: center;
}

.logout-button:hover {
    border-color: var(--danger);
    color: var(--danger);
}

.admin-main {
    min-width: 0;
    padding: 36px;
    max-width: 1500px;
    width: 100%;
    margin: 0 auto;
}

.admin-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 20px;
    margin-bottom: 28px;
}

.eyebrow {
    color: var(--accent);
    font-size: 12px;
    font-weight: 800;
    letter-spacing: 0.16em;
}

h1,
h2,
p {
    margin-top: 0;
}

h1 {
    margin-bottom: 8px;
    font-size: clamp(28px, 4vw, 44px);
}

h2 {
    margin-bottom: 6px;
    font-size: 19px;
}

.admin-header p,
.panel-heading p {
    margin-bottom: 0;
    color: var(--muted);
}

.view-site-link {
    color: var(--accent);
    text-decoration: none;
    padding: 10px 14px;
    border: 1px solid var(--border);
    border-radius: 12px;
}

.metric-grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 16px;
    margin-bottom: 18px;
}

.metric-card,
.panel {
    border: 1px solid var(--border);
    border-radius: var(--radius);
    background: linear-gradient(145deg, var(--surface), rgba(13, 27, 46, 0.72));
    box-shadow: 0 18px 45px rgba(0, 0, 0, 0.16);
}

.metric-card {
    padding: 20px;
}

.metric-card span {
    display: block;
    color: var(--muted);
    font-size: 13px;
    margin-bottom: 12px;
}

.metric-card strong {
    font-size: 30px;
}

.panel {
    padding: 22px;
    margin-bottom: 18px;
}

.panel-heading {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 20px;
    margin-bottom: 18px;
}

.panel-heading.compact {
    margin-bottom: 12px;
}

.range-buttons,
.feedback-filters {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
}

.range-button,
.filter-button {
    border: 1px solid var(--border);
    background: transparent;
    color: var(--muted);
    border-radius: 9px;
    padding: 8px 11px;
}

.range-button.active,
.filter-button.active,
.range-button:hover,
.filter-button:hover {
    color: var(--text);
    border-color: rgba(85, 199, 255, 0.45);
    background: rgba(85, 199, 255, 0.1);
}

.chart-wrapper {
    position: relative;
    height: 330px;
}

#trafficChart {
    width: 100%;
    height: 100%;
    display: block;
}

/*
 * EMPTY STATES
 * ------------------------------------------------------------
 * The traffic chart empty message needs absolute positioning
 * inside .chart-wrapper.
 *
 * Feedback empty messages must remain in normal document flow.
 * Otherwise an absolute empty-state can stretch over the
 * feedback filter buttons on mobile and block later taps.
 * ------------------------------------------------------------
 */
.chart-wrapper .empty-state {
    position: absolute;
    inset: 0;
    display: grid;
    place-items: center;
    color: var(--muted);
}

.feedback-list .empty-state {
    position: static;
    inset: auto;
    display: block;
    min-height: 80px;
    padding: 24px 8px;
    color: var(--muted);
    text-align: center;
}

.two-column-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 18px;
}

.data-list {
    display: grid;
    gap: 10px;
}

.page-row {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 15px;
    align-items: center;
    padding: 12px 0;
    border-bottom: 1px solid var(--border);
}

.page-row:last-child {
    border-bottom: 0;
}

.page-row span {
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.page-row strong {
    color: var(--accent);
}

.info-list {
    margin: 0;
    padding-left: 20px;
    color: var(--muted);
    line-height: 1.8;
}

.feedback-filters {
    margin-bottom: 16px;
}

.feedback-list {
    display: grid;
    gap: 12px;
}

.feedback-card {
    padding: 16px;
    border: 1px solid var(--border);
    border-radius: 14px;
    background: rgba(255, 255, 255, 0.02);
}

.feedback-meta {
    display: flex;
    justify-content: space-between;
    gap: 10px;
    margin-bottom: 8px;
    color: var(--muted);
    font-size: 13px;
}

.feedback-card p {
    margin-bottom: 0;
    line-height: 1.65;
    white-space: pre-wrap;
}

@media (max-width: 1050px) {
    .metric-grid {
        grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .two-column-grid {
        grid-template-columns: 1fr;
    }
}

@media (max-width: 760px) {
    /*
     * Keep feedback filters above the message list/empty state so
     * every filter remains tappable after switching categories.
     */
    .feedback-filters {
        position: relative;
        z-index: 2;
    }

    .filter-button {
        position: relative;
        z-index: 3;
        touch-action: manipulation;
    }

    .admin-shell {
        grid-template-columns: 1fr;
    }

    .admin-sidebar {
        border-right: 0;
        border-bottom: 1px solid var(--border);
    }

    .admin-sidebar nav {
        display: flex;
        overflow-x: auto;
    }

    .nav-item {
        min-width: 150px;
    }

    .logout-button {
        margin-top: 0;
    }

    .admin-main {
        padding: 22px 15px;
    }

    .admin-header,
    .panel-heading {
        flex-direction: column;
        align-items: stretch;
    }

    .metric-grid {
        grid-template-columns: 1fr 1fr;
    }
}

@media (max-width: 480px) {
    .metric-grid {
        grid-template-columns: 1fr;
    }

    .chart-wrapper {
        height: 250px;
    }
}


/*
 * ============================================================
 * DATA MANAGEMENT / CLEANUP CONTROLS
 * ------------------------------------------------------------
 * These rules style only the Data Management section.
 * They match the existing Admin Dashboard dark theme without
 * changing any cleanup JavaScript, API calls, or HTML structure.
 * ============================================================
 */

.cleanup-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 18px;
    margin-top: 18px;
}

.cleanup-card,
.cleanup-custom {
    border: 1px solid var(--border);
    border-radius: 16px;
    background: rgba(255, 255, 255, 0.025);
    padding: 20px;
}

.cleanup-card h3,
.cleanup-custom h3 {
    margin: 0 0 8px;
    font-size: 17px;
    color: var(--text);
}

.cleanup-card p,
.cleanup-custom > p {
    margin: 0 0 18px;
    color: var(--muted);
    line-height: 1.6;
}

.cleanup-card label {
    display: block;
    margin-bottom: 8px;
    color: var(--text);
    font-size: 14px;
    font-weight: 700;
}

.cleanup-card select {
    display: block;
    width: 100%;
    min-height: 44px;
    margin-bottom: 14px;
    padding: 10px 40px 10px 12px;
    border: 1px solid var(--border);
    border-radius: 10px;
    background: var(--surface-2);
    color: var(--text);
    font: inherit;
    cursor: pointer;
}

.cleanup-card select:hover,
.cleanup-card select:focus {
    border-color: rgba(85, 199, 255, 0.5);
    outline: none;
}

.danger-button {
    min-height: 42px;
    padding: 10px 14px;
    border: 1px solid rgba(255, 123, 136, 0.35);
    border-radius: 10px;
    background: rgba(255, 123, 136, 0.09);
    color: var(--danger);
    font-weight: 700;
    transition: background 0.18s ease, border-color 0.18s ease, transform 0.18s ease;
}

.danger-button:hover {
    background: rgba(255, 123, 136, 0.16);
    border-color: rgba(255, 123, 136, 0.65);
}

.danger-button:active {
    transform: translateY(1px);
}

.cleanup-custom {
    margin-top: 18px;
}

.date-fields {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 14px;
    margin-bottom: 16px;
}

.date-fields label {
    display: grid;
    gap: 8px;
    color: var(--text);
    font-size: 14px;
    font-weight: 700;
}

.date-fields input {
    width: 100%;
    min-height: 44px;
    padding: 9px 11px;
    border: 1px solid var(--border);
    border-radius: 10px;
    background: var(--surface-2);
    color: var(--text);
    font: inherit;
    color-scheme: dark;
}

.date-fields input:focus {
    border-color: rgba(85, 199, 255, 0.5);
    outline: none;
}

.cleanup-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
}

.cleanup-status {
    min-height: 22px;
    margin: 18px 0 0;
    color: var(--accent);
    font-size: 14px;
    line-height: 1.5;
}

.cleanup-warning {
    margin: 10px 0 0;
    color: var(--muted);
    font-size: 13px;
    line-height: 1.5;
}

/*
 * Native select option visibility fix.
 * The rule is intentionally scoped to the admin content area.
 */
.admin-main select,
.admin-main select option {
    color: var(--text);
    background-color: var(--surface-2);
}

.admin-main select option:checked {
    color: var(--text);
    background-color: var(--surface-2);
}

.admin-main select:focus {
    color: var(--text);
    background-color: var(--surface-2);
}

@media (max-width: 760px) {
    .cleanup-grid {
        grid-template-columns: 1fr;
    }

    .cleanup-card,
    .cleanup-custom {
        padding: 17px;
    }

    .cleanup-card select {
        width: 100%;
    }

    .date-fields {
        grid-template-columns: 1fr;
        gap: 12px;
    }

    .cleanup-actions {
        display: grid;
        grid-template-columns: 1fr;
    }

    .cleanup-actions .danger-button,
    .cleanup-card .danger-button {
        width: 100%;
    }
}

@media (max-width: 480px) {
    .cleanup-card,
    .cleanup-custom {
        padding: 15px;
        border-radius: 14px;
    }

    .cleanup-card h3,
    .cleanup-custom h3 {
        font-size: 16px;
    }

    .cleanup-card p,
    .cleanup-custom > p {
        font-size: 14px;
    }
}
